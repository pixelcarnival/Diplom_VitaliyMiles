import * as users from './fake.api/users.api'
import * as formats from './fake.api/formats.api'
import * as genres from './fake.api/genres.api'

const API = {
    users,
    genres,
    formats
}

export default API
